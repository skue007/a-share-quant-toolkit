# 联动选股 — 环境变量配置
# 复制此文件为 .env 并填入你的实际路径（.env 已被 git 忽略，不会提交）

# ── 通达信数据目录（必填，日线数据来源）────────────────────
# 指向通达信安装目录，需包含 vipdoc/sh/lday、vipdoc/sz/lday 等子目录
TDX_ROOT=D:/program/通达信

# ── （可选）板块指数联动模式的数据目录 ─────────────────────
# 默认读取 {TDX_ROOT}/vipdoc/sh/lday 下的 sh880xxx.day 板块指数文件
# 无需单独配置
