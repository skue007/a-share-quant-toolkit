# 联动选股 — 依赖清单
# 安装：pip install -r requirements.txt

streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# 可选：网络数据源（无通达信本地数据时使用 baostock 获取日线/行业分类）
baostock>=0.8.8

# 可选：构建板块联动网络图（NetworkX）时需要
networkx>=3.0
